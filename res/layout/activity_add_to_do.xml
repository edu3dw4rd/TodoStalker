<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".AddToDoActivity" >

    <Spinner
        android:id="@+id/preferredLocationSpinner"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/categorySpinner"
        android:layout_below="@+id/categorySpinner"
        android:layout_marginTop="29dp" />

    <Spinner
        android:id="@+id/categorySpinner"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="24dp" />

    <EditText
        android:id="@+id/notesTxtBox"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/preferredLocationSpinner"
        android:layout_alignRight="@+id/preferredLocationSpinner"
        android:layout_below="@+id/preferredLocationSpinner"
        android:layout_marginTop="60dp"
        android:ems="10"
        android:hint="@string/noteText" >

        <requestFocus />
    </EditText>

</RelativeLayout>
